<% title "edit #{@bowl.name}" %>

<div class="column">
  <h2>edit '<%= @bowl.name %>'</h2>

  <%= form_for @bowl do |f| %>
    <%= render 'form', f: f %>
  <% end %>

  <p><%= link_to "delete bowl", @bowl, method: :delete, data: { confirm: "really?" } %></p>
